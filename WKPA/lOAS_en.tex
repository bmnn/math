For the logarithm with base $b$ and real numbers $x,y \in \mathbb{R}_+$ the following equalities hold: 
$$\log_b(x y) = \log_b(x) + \log_b(y)$$ % product of arguments
$$\log_b(\frac{x}{y}) = \log_b(x) - \log_b(y)$$ % quotient of arguments
$$\log_b(x^p) = p \log_b(x)$$ % power
